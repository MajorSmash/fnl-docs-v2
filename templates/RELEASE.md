---
doc_type: RELEASE
title: "EXAMPLE — FluidNinja LIVE v2 release"
version: "0.0.0-EXAMPLE"
date: 1970-01-01
ue_versions: ["5.x"]
source_url: "https://discord.com/channels/EXAMPLE/EXAMPLE/EXAMPLE"
---

## Added
- EXAMPLE — new feature, one per bullet.

## Changed
- EXAMPLE — changed behaviour, one per bullet.

## Removed
- EXAMPLE — removed feature (these feed the Kill Switch, §2.5).

## Fixed
- EXAMPLE — bug fix, one per bullet.

<!--
  RELEASE exemplar. EXAMPLE VALUES ONLY — every value above is a sentinel so this
  file cannot declare a real version even if the ingestion pipeline's template
  skip ever fails (defence in depth; the pipeline MUST NOT ingest templates/ or
  _-prefixed files — see CONTRIBUTING.md).

  The releases/ folder is the ONLY place a product version is declared (§2.1).
  To add a real release, copy releases/_TEMPLATE.md into releases/<version>.md
  and replace every value. See CONTRIBUTING.md.
-->
