---
title: Parameter reference
description: Browse and search FluidNinja LIVE 2 component and actor parameters.
sidebar:
  hidden: true
---

import ParameterReference from '../../../components/ParameterReference.astro';
import { sitePath } from '../../../lib/site-path.mjs';

Browse the parameters exposed by NinjaLive Component and NinjaLive Actor. Filter by a parameter name or words from its description, or jump straight to a group; every parameter heading is a permanent deep link.

<ParameterReference />

The <a href={sitePath('descriptors/ninjalive2-content/')}>content descriptor</a> remains available in its canonical machine-oriented form. Its folder/level/stage/setup hierarchy is a different grammar from parameter declarations, so it is not interpreted by this parameter parser.
