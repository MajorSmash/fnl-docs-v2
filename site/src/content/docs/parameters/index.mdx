---
title: Parameters & descriptors
description: Canonical parameter, content, and limitations references for FluidNinja LIVE 2.
sidebar:
  hidden: true
---

import DescriptorIndex from '../../../components/DescriptorIndex.astro';

Use these references when you need exact parameter names, project content locations, or a precise statement of current limitations.

The descriptor grammar is preserved in source and presented here as structured, scannable list items:

- `{G.}` identifies a parameter group.
- `{P.}` identifies an individual parameter and its metadata.

<DescriptorIndex />
